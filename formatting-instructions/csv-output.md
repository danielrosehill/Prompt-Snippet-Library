# CSV Output Formatting Snippet

## Text Version
```
Format your output as CSV data within a code block. Include appropriate headers in the first row and ensure all data is properly escaped and comma-separated. Use double quotes around fields that contain commas, quotes, or line breaks.
```

## JSON Integration Example
```json
{
  "system_prompt": "Format your output as CSV data within a code block. Include appropriate headers in the first row and ensure all data is properly escaped and comma-separated. Use double quotes around fields that contain commas, quotes, or line breaks. [Additional system instructions continue here...]",
  "user_prompt": "Your user prompt here"
}
```

## Usage Notes
- Produces immediately importable CSV data
- Handles special characters and escaping automatically
- Perfect for data analysis and spreadsheet import
- Code block format prevents formatting issues in various interfaces
- Compatible with Excel, Google Sheets, and database import tools
